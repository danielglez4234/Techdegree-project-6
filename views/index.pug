extends layout.pug

block content
  article.article_backg_index.grid-container.portfolio-intro
    div

      h1.h1_index_animation My Portfolio ✮

      p.h1_index_animation.lead.text-light
        | Hey!! You want to see some cool projects?  You are in the right place!
  article.grid-container.portfolio-index

    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3 

    each projects in project
      .cell.cell_animation
        a(href=`/projects/${projects.id}`)
          img.thumbnail(src=`/static/${projects.image_urls[0]}`)
          h5 #{projects.project_name}
